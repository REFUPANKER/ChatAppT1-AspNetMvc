@model object;
@{
	Layout = "";
}

<div id="profileItem" style="z-index:99;background-color:#000000ba;" class="w-100 h-100 top-0 start-0 position-fixed overflow-hidden d-flex justify-content-center align-items-center animated fadeIn">
	<div class="animated zoomIn w-50 h-75 bg-secondary rounded-3 shadow-lg border border-2 border-light p-3">
		<div class="d-flex flex-row">
			<h1 class="w-100 text-center ">Show profile</h1>
			<button onclick="closeProfileClick()" class="btn btn-danger h-100">X</button>
		</div>
		User token : @Model
	</div>
</div>
